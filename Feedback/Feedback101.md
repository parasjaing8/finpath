## Analysis Table

| # | Feedback Item | Importance | Root Cause / Current State | Suggestion | Implementation Plan (for LLM) | Status |
|---|---|---|---|---|---|---|
| 1 | **DOB default in create-profile scrolls too much** | 🔴 High | `app/onboarding/create-profile.tsx` line 20: `useState('2000-01-01')`. DateInput spinner starts at year 2000, requiring heavy scroll to reach ~1995. | Change default DOB to `1995-06-01` (age ~30, avg FinPath user). | In `app/onboarding/create-profile.tsx`, change line 20 from `useState('2000-01-01')` to `useState('1995-06-01')`. Also check `components/DateInput.tsx` — if it accepts an `initialDate` or `date` prop, ensure it initializes the picker wheel at the value passed, not a hardcoded year. | todo |
| 2 | **Expense categories missing / default inflation rate same for all** | 🔴 High | `constants/categories.ts` EXPENSE_CATEGORIES already has: RENT (6%), EMI (0%), GROCERIES (6%), TRANSPORT (6%), EDUCATION (10%), MEDICAL (8%), EVENTS (6%), INSURANCE (5%), PENSION_INCOME (6%), OTHERS (6%). The problem is: (a) the UI for adding expenses may not show category selector at all or shows it poorly, and (b) when a category is selected, the `inflation_rate` field is NOT auto-populated from `defaultInflation`. | Auto-populate `inflation_rate` when user selects a category. Display the full category list clearly. | In `app/(tabs)/expenses.tsx`, find where category is selected. On category change: `setForm(f => ({ ...f, category: key, inflation_rate: EXPENSE_CATEGORIES.find(c => c.key === key)?.defaultInflation ?? 6 }))`. Check that the expense add form renders a category picker using `EXPENSE_CATEGORIES` (same pattern as assets page uses `ASSET_CATEGORIES`). If category picker is missing from expenses form, add it using a `SegmentedButtons` or row-chip layout identical to assets. | todo |
| 3 | **No calendar/date picker for expense entries** | 🔴 High | `components/DateInput.tsx` exists and is used in create-profile. Expense form in `app/(tabs)/expenses.tsx` uses plain `TextInput` for `start_date` / `end_date` with no `DateInput` component. | Replace the `start_date` and `end_date` `TextInput` fields in the expense form with the existing `<DateInput>` component. | In `app/(tabs)/expenses.tsx`: (1) `import { DateInput } from '../../components/DateInput'`. (2) Find the `start_date` and `end_date` `TextInput` renders inside the add/edit expense form. Replace each with `<DateInput label="Start Date" value={form.start_date ?? ''} onChange={v => setForm(f => ({ ...f, start_date: v }))} />` and same for `end_date`. These fields should only show when `expense_type` is `FUTURE_ONE_TIME` or `FUTURE_RECURRING` (already conditionally rendered via `form.expense_type !== 'FUTURE_ONE_TIME'` guard on line 276 — verify and adjust). | todo |
| 4 | **Future One-Time expense tile shows 'Quarterly'** | 🟡 Medium | `app/(tabs)/expenses.tsx` line 140: `{e.frequency ? \`${e.frequency.charAt(0) + e.frequency.slice(1).toLowerCase()} · \` : ''}` — this renders frequency for ALL expense types including `FUTURE_ONE_TIME`, which has no meaningful frequency but the DB field may still hold a value from a previous entry or default. | Suppress frequency display when `expense_type === 'FUTURE_ONE_TIME'`. | In `app/(tabs)/expenses.tsx` at line 140, change the frequency render to: `{e.expense_type !== 'FUTURE_ONE_TIME' && e.frequency ? \`${e.frequency.charAt(0) + e.frequency.slice(1).toLowerCase()} · \` : ''}`. Also ensure that when `expense_type` is set to `FUTURE_ONE_TIME` in the form, `frequency` is cleared: add `setForm(f => ({ ...f, expense_type: t.key, ...(t.key === 'FUTURE_ONE_TIME' ? { frequency: undefined } : {}) }))` in the type-select handler. | todo |
| 5 | **Asset name placeholder 'HDFC Flexi Cap' is static** | 🟡 Medium | `app/(tabs)/assets.tsx` line 237: `placeholder="e.g., HDFC Flexi Cap"` is hardcoded regardless of category. | Make placeholder dynamic based on selected `form.category`. | In `app/(tabs)/assets.tsx`, define a lookup map above the component: `const ASSET_NAME_PLACEHOLDER: Record<string, string> = { ESOP_RSU: 'e.g., Infosys RSU', STOCKS: 'e.g., Reliance Industries', MUTUAL_FUND: 'e.g., HDFC Flexi Cap', SAVINGS: 'e.g., SBI Savings Account', GOLD_SILVER: 'e.g., SGB 2024', PF: 'e.g., EPFO', NPS: 'e.g., NPS Tier 1', REAL_ESTATE: 'e.g., Flat in Bangalore', OTHERS: 'e.g., Startup Investment', EPF: 'e.g., EPFO Account' }`. Then replace the static `placeholder="e.g., HDFC Flexi Cap"` with `placeholder={ASSET_NAME_PLACEHOLDER[form.category] ?? 'e.g., Asset Name'}`. | todo |
| 6 | **Expected ROI % and Inflation Rate % should be sliders (0–20)** | 🟡 Medium | Both `expected_roi` in assets form and `inflation_rate` in expenses form are plain numeric `TextInput` fields. Goals page already uses `CustomSlider` component (`components/CustomSlider.tsx` via `@/components/CustomSlider`). | Replace both fields with `CustomSlider` range 0–20. Keep a numeric readout next to the slider. | **Assets (`app/(tabs)/assets.tsx`):** Import `CustomSlider`. Replace the `expected_roi` TextInput with: `<CustomSlider value={form.expected_roi} onValueChange={v => setForm(f => ({ ...f, expected_roi: Math.round(v * 10) / 10 }))} minimumValue={0} maximumValue={20} step={0.5} />` and show `{form.expected_roi}%` as a label. **Expenses (`app/(tabs)/expenses.tsx`):** Same for `inflation_rate` field. Use `minimumValue={0}` `maximumValue={20}` `step={0.5}`. Show `{form.inflation_rate}%` label. Both fields: when category is changed and inflation is auto-set (item #2), the slider will reflect the new value automatically. | todo |
| 7 | **Goals page: section order — Retirement Plan card should come before FIRE Strategy** | 🟡 Medium | `app/(tabs)/goals.tsx`: FIRE Strategy card is rendered first (line 65), Retirement Age card second (line 91). User expects to see Retirement Plan (sliders) first, then FIRE strategy below. Also the card title "Retirement Age" should read "Retirement Plan". | Reorder cards: Retirement Plan first, FIRE Strategy second. Rename title. | In `app/(tabs)/goals.tsx`: (1) Rename line 93 sectionTitle from `'Retirement Age'` to `'Retirement Plan'`. (2) Move the entire FIRE Strategy `<View style={[styles.card]}> ... </View>` block (lines 65–89) to appear AFTER the Retirement Age card (currently ends around line 127). Cut the FIRE Strategy card JSX and paste it between the Retirement Age card closing `</View>` and the Withdrawal Target card opening `<View>`. | todo |
| 8 | **Goals page: FIRE strategy should be inside Withdrawal Target card with renamed options** | 🟡 Medium | Currently FIRE Strategy is a separate card with options: `slim` (Lean FIRE), `moderate` (FIRE), `fat` (Fat FIRE). User wants: (a) move it inside Withdrawal Target card, (b) rename `moderate` label from `'FIRE'` to `'Moderate'` or `'Comfortable FIRE'`, (c) position it between 'monthly withdrawal in today's money' and inflation rate slider. | Merge FIRE type selector into Withdrawal Target card. Rename 'FIRE' → 'Moderate FIRE'. | In `app/(tabs)/goals.tsx`: (1) In `FIRE_TYPES` array (line 13–17), change `{ key: 'moderate', label: 'FIRE', ... }` to `{ key: 'moderate', label: 'Moderate FIRE', ... }`. (2) Cut the FIRE Strategy card's inner content (the `FIRE_TYPES.map(...)` render) and paste it inside the Withdrawal Target card (`<View style={[styles.card]}>` starting line 129), positioned after the `pension_income` slider row and before the `inflation_rate` slider row. Remove the now-empty FIRE Strategy card wrapper. Update the `sectionTitle` inside Withdrawal Target or add a smaller sub-label `<Text style={styles.subLabel}>FIRE Strategy</Text>` above the type selector. | todo |
| 9 | **'Save Goals' button should be renamed 'Save Goals and Calculate' and navigate to dashboard** | 🔴 High | `app/(tabs)/goals.tsx` line 195: `<Text style={styles.saveBtnText}>Save Goals</Text>`. The button saves goals to DB/context but does not trigger a dashboard recalculation or navigate there. | Rename button. After save, navigate to `/(tabs)/dashboard`. | In `app/(tabs)/goals.tsx`: (1) Line 195: change text to `'Save Goals and Calculate'`. (2) In the button's `onPress` handler (after `setGoals(...)` and `setSaved(true)`), add navigation: `import { useRouter } from 'expo-router'` at top, then after save call: `router.push('/(tabs)/dashboard')`. The 5-second loading delay (item #10) will handle the transition animation. | todo |
| 10 | **5-second loading animation between Goals save and Dashboard** | 🟡 Medium | No loading state exists between saving goals and dashboard render. Dashboard re-calculates synchronously from context on mount. User wants a deliberate 5-second loading screen to signal "calculating". | Show a full-screen loading overlay for exactly 5 seconds after 'Save Goals and Calculate', then navigate to dashboard. | Two approaches — **Option A (Goals page):** After saving goals, set a local `calculating` state, show a full-screen `Modal` or overlay with `ActivityIndicator` + "Calculating your FIRE plan..." text, use `setTimeout(() => { setCalculating(false); router.push('/(tabs)/dashboard'); }, 5000)`. **Option B (Dashboard page):** Accept a route param `?fresh=1`, on mount if `fresh=1` show a loading overlay for 5 seconds before displaying content. **Recommended: Option A** — self-contained in goals.tsx, no param passing needed. Add a `calculating` boolean state. Overlay: `<Modal visible={calculating} transparent><View style fullscreen centered dark-50%-bg><ActivityIndicator size="large" color="#1B5E20"/><Text>Calculating your FIRE plan…</Text></View></Modal>`. | todo |
| 11 | **Dashboard HeroCard: duplicate 'on track' and 'lasts till 100' info** | 🟢 Low | `app/(tabs)/dashboard.tsx`: `planStatus` object (line 149–163) generates `title: "You're on track"` and `subtitle: "Money lasts till 100"`. These are passed to `<HeroCard planStatus={planStatus}>` which renders them as subtitle text (line 48 in HeroCard). Additionally, `<HeroCard isOnTrack={isOnTrack}>` shows an "✓ On Track" pill (line 51 in HeroCard). When user adjusts SIP slider, the pill shows actual SIP vs required SIP status, while the subtitle shows the Goals-based projection — these are validly different dimensions. | Leave as-is OR consolidate: hide the subtitle text from HeroCard when the pill already covers the same info (i.e. when `isOnTrack && !failureAge`), preventing the perception of redundancy. | **If optimizing:** In `components/HeroCard.tsx`, conditionally render the `planStatus.subtitle` text only when `!isOnTrack` or `failureAge > 0`. The "✓ On Track" pill already communicates the positive state. So line 48: change to `{(!isOnTrack \|\| failureAge > 0) && <Text style={styles.heroSubtitle}>{planStatus.subtitle}</Text>}`. This keeps the subtitle visible only when it adds new information (failure or off-track). **If leaving as-is:** Both indicators are independently useful and not truly redundant — leave unchanged. User's call. | todo |