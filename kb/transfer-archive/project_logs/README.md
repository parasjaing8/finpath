# Finpath — Project Briefing

> Quick-start for any Claude session picking up Finpath work.

**What it is:** Personal finance / FIRE (Financial Independence, Retire Early) calculator. Android-only app, local-first, no backend.

**Current version:** 1.0.1 (versionCode 2)
**Latest APK:** `C:\dropbox\Finpath\FinPath-v1.0.1-r3.apk`
**Package name:** `com.anonymous.finpath` (preserved for upgrade compatibility — do not change)

---

## Where everything lives

| Thing | Location |
|---|---|
| Source code | Mac mini: `~/finpath/` |
| Git remote | GitHub (check `git remote -v` on Mac mini) |
| Release keystore | Mac mini: `~/finpath/android/app/finpath-release.jks` |
| Knowledge base | Mac mini: `~/finpath/kb/` |
| APK outputs | `C:\dropbox\Finpath\` |

---

## How to pick up where you left off

```bash
ssh parasjain@192.168.0.130
cd ~/finpath
git log --oneline -10
git status
```

Then read `projects/finpath/status.md` for current known issues and next steps.

---

## Key files on Mac mini

```
~/finpath/
├── app/
│   ├── (tabs)/
│   │   ├── _layout.tsx       # Tab order and icons
│   │   ├── assets.tsx        # Asset tracking screen
│   │   ├── expenses.tsx      # Expense tracking
│   │   ├── goals.tsx         # FIRE goals
│   │   ├── dashboard.tsx     # Net worth + charts
│   │   └── profile.tsx       # User profile + settings
│   └── _layout.tsx           # Root layout (auth gate)
├── components/
│   ├── DateInput.tsx         # Pure-JS date picker (bottom-sheet modal)
│   └── ...
├── plugins/
│   └── withReleaseSigning.js # Expo config plugin for Android signing
├── kb/                       # Authoritative docs — read before touching logic
│   ├── ARCHITECTURE.md
│   ├── FINANCIAL_MODEL.md
│   └── DECISIONS_AND_LESSONS.md
├── app.json                  # Expo config (version, package name, plugins)
└── android/                  # Generated by expo prebuild — do not manually edit
```

---

## Tab order (left → right)

Assets → Expenses → Goal → Dashboard → Profile

---

## Before touching calculator/finance logic

**Always read `~/finpath/kb/` first.** The financial model is specific and decisions there are intentional.
